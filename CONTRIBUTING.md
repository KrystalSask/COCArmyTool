# 参与 COCArmyTool

欢迎提交问题报告和非商业用途的改进。参与前请先阅读 `docs/PROJECT.md`、`docs/MAINTENANCE.md` 和 `LICENSE`。

## 开发流程

1. 从 `main` 创建主题分支。
2. 只提交与目标相关的源文件、测试和文档。
3. 为行为变化补充相应测试。
4. 至少运行 `npm test` 和 `npm run build`；涉及用户流程时运行 `npm run test:e2e`。
5. Pull Request 说明问题、实现、验证结果以及数据/兼容性影响。

## 重要约束

- 保持解析、校验和界面门槛分层。
- 不破坏旧 IndexedDB 名称或备份格式。
- 不在测试、报告或源码中提交个人绝对路径。
- 新样本必须经过隐私和重复哈希检查。
- 新第三方代码、数据或素材必须记录来源与许可。
- 项目许可限制商业用途；提交即表示你有权提供相关改动，并同意其按项目许可发布。

大规模协议、许可、数据模型或样本范围变化应先开 Issue 讨论。
