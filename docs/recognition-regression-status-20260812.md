# 识别回归状态（2026-08-12）

## 当前可重复验证结果

- 真实样本：13 张（7 张 iPhone 横屏、6 张 iPad 横屏）。
- 原始样本回归：13/13 面板定位、卡片数量、英雄子卡片回归通过。
- 正式格式派生基准（样本 001）：PNG、JPEG 95、JPEG 85、WebP 90 的面板定位与卡片切分结果等价。
- 尺寸派生基准（样本 001）：75% 缩放结果等价。
- 50% 缩放压力样本：检测 24/25 张卡片（96%），未达到 98% 目标；结果保持未确认状态，不能直接进入编辑器或导出。

## 结论边界

当前真实设备样本只覆盖两类 Apple 横屏布局，因此不能宣称多设备真实泛化率达到 98%。派生样本只用于测量格式、压缩和缩放鲁棒性，不替代新的真实设备样本。

## 可重复命令

```powershell
npm run samples:variants
npx playwright test e2e/recognition-variants.spec.ts
npx playwright test e2e/mvp.spec.ts --grep "13 个原始样本"
```

下一步识别优化重点是 50% 缩放下的数量角标与末尾卡片检测；在达到门槛前继续保留人工确认和领域校验安全门槛。
